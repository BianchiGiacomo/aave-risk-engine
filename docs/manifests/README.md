# Run Manifests

The July 30 pair preserves the earlier research vintage, while the August 17
pair is the `v0.1.0` publication evidence. Each manifest records its command,
snapshot hash, block, parameters, seed, results, cap sweep, and clearance test.
